@Component
    when the app starts, springBoot build the instance
    of the Class and store it in spring container
wherever the instance needed the springBoot automatically 
provide itself , we don't need to create and pass.
that's called Ioc.

it is happening when the ComponentScan.